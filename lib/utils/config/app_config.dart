class AppConfig {
  static const baseUrl = 'https://api.spotify.com/v1';
  static const baseUrlToken = 'https://accounts.spotify.com/api/token';
  // should be in env
  static const clientSecret = '';
  static const clientID = '';
}
